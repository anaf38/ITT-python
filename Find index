haystack = input("Enter haystack:")
needle = input("Enter needle:")
m = len(haystack)    
n = len(needle)      
result = -1

for i in range(m - n + 1):
    for j in range(n):
        if needle[j] != haystack[i + j]:
            break
    else:
        result = i
        break

print(result)
